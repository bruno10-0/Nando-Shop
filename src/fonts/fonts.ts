import { Montserrat } from "next/font/google";

export const montserrat = Montserrat({subsets:['latin'], weight:'400'});

import { Poiret_One } from "next/font/google";

export const poiret_one = Poiret_One({subsets:['latin'], weight:'400'});

import { Anton } from "next/font/google";

export const anton = Anton({subsets:['latin'], weight:'400'});

import { Mitr } from "next/font/google";

export const mitr = Mitr({subsets:['latin'], weight:'400'});

import { Bungee } from "next/font/google";

export const bungee = Bungee({subsets:['latin'], weight:'400'});

import { Paytone_One } from "next/font/google";

export const paytone_One = Paytone_One({subsets:['latin'], weight:'400'});

